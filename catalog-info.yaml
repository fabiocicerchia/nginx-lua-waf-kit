# Backstage catalog entry, read by paved's scorecard plugin.
#
# Every check paved scores is derived from this file alone — no cluster
# access, no CI credentials. What is absent here is absent from the score.
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: nginx-lua-waf-kit
  description: >-
      Opt-in Lua modules for the nginx-lua image: shared-dict rate limiting, bot scoring, edge JWT
      validation, geo/ASN policy and request mirroring. A kit, not a WAF product.
  annotations:
    github.com/project-slug: fabiocicerchia/nginx-lua-waf-kit
    backstage.io/source-location: url:https://github.com/fabiocicerchia/nginx-lua-waf-kit/
    backstage.io/techdocs-ref: dir:.
  links:
    - url: https://github.com/fabiocicerchia/nginx-lua-waf-kit
      title: Source
spec:
  type: library
  lifecycle: production
  owner: group:default/kodebeat
  system: oss-tools
