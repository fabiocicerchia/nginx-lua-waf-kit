# TODO

Open items only. Completed work is dropped from here — the CHANGELOG
is the record of what shipped.

- [ ] Run `bench/run.sh` on hardware worth quoting and commit `bench/results.md` — the release blocker: this audience will not put Lua in the request path on faith.
- [ ] Exercise the modules under a live OpenResty; the first deployment should be `log_only` everywhere.
- [ ] Publish one example config tag per nginx-lua release.
